export enum AppRoute {
  Favorites = '/favorites',
  Login = '/login',
  Root = '/',
  Offer = '/offer/:id',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}

export enum City {
  paris = 'Paris',
  cologne = 'Cologne',
  brussels = 'Brussels',
  amsterdam = 'Amsterdam',
  hamburg = 'Hamburg',
  dusseldorf = 'Dusseldorf',
}
